<%= form_for [@parent, @comment], :remote => remote do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id  %>
  <p class="text_area">
    <%= f.label :body, "Comment	"  %>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit "Submit" %>
  </p>
<%end%>